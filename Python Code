#Left Rotation of an array
def rotateLeft(d, arr):
    return arr[d:] + arr[:d]
    
#Sparse Array (Problem statement define in C code line: 175 )
def matchingStrings(strings, queries):
    match=[]
    for i in queries:
        match.append(strings.count(i))
    return match
